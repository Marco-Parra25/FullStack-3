# Mock Server Configuration for MS Notificaciones

# Server configuration
mockserver.port=1080
mockserver.logLevel=INFO

# CORS configuration
mockserver.cors.allowedOrigins=*
mockserver.cors.allowedMethods=GET,POST,PUT,DELETE,OPTIONS
mockserver.cors.allowedHeaders=*

# Metrics configuration
mockserver.metrics.enabled=true

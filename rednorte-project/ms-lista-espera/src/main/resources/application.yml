spring:
  application:
    name: ms-lista-espera

  datasource:
    url: jdbc:postgresql://localhost:5432/lista_espera_db
    username: rednorte
    password: rednorte123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8081

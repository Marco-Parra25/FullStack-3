---
# ServiceAccount para Grafana
apiVersion: v1
kind: ServiceAccount
metadata:
  name: grafana
  namespace: rednorte
  labels:
    app: grafana
    component: rbac
  annotations:
    description: "ServiceAccount para Grafana con permisos de monitoreo"

---
# Role para Grafana (namespace scope)
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: grafana
  namespace: rednorte
  labels:
    app: grafana
    component: rbac
  annotations:
    description: "Role con permisos para Grafana en namespace rednorte"

rules:
# Permisos para leer recursos de monitoreo
- apiGroups: [""]
  resources: ["pods", "services", "endpoints", "configmaps", "secrets"]
  verbs: ["get", "list", "watch"]

# Permisos para leer deployments y replicasets
- apiGroups: ["apps"]
  resources: ["deployments", "replicasets", "replicationcontrollers"]
  verbs: ["get", "list", "watch"]

# Permisos para leer HPA
- apiGroups: ["autoscaling"]
  resources: ["horizontalpodautoscalers"]
  verbs: ["get", "list", "watch"]

# Permisos para leer batch jobs
- apiGroups: ["batch"]
  resources: ["jobs", "cronjobs"]
  verbs: ["get", "list", "watch"]

# Permisos para leer networking
- apiGroups: ["extensions", "networking.k8s.io"]
  resources: ["ingresses", "networkpolicies"]
  verbs: ["get", "list", "watch"]

---
# RoleBinding para Grafana
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: grafana
  namespace: rednorte
  labels:
    app: grafana
    component: rbac
  annotations:
    description: "Binding del Role de Grafana al ServiceAccount"

subjects:
- kind: ServiceAccount
  name: grafana
  namespace: rednorte

roleRef:
  kind: Role
  name: grafana
  apiGroup: rbac.authorization.k8s.io

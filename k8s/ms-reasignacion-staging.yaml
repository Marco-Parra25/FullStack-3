apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-reasignacion
  namespace: rednorte-staging
  labels:
    app: ms-reasignacion
    environment: staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ms-reasignacion
  template:
    metadata:
      labels:
        app: ms-reasignacion
        environment: staging
    spec:
      containers:
      - name: ms-reasignacion
        image: ghcr.io/marco-parra25/fullstack-3/ms-reasignacion:latest
        ports:
        - containerPort: 8082
        env:
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://postgres-reasignacion:5432/reasignacion_db
        - name: SPRING_DATASOURCE_USERNAME
          value: rednorte
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rednorte-secrets
              key: postgres-password
        - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          value: kafka:9092
        - name: SERVER_PORT
          value: "8082"
        - name: SPRING_PROFILES_ACTIVE
          value: "staging"
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ms-reasignacion
  namespace: rednorte-staging
  labels:
    app: ms-reasignacion
    environment: staging
spec:
  selector:
    app: ms-reasignacion
  ports:
  - port: 8082
    targetPort: 8082
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ms-reasignacion-hpa
  namespace: rednorte-staging
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ms-reasignacion
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
